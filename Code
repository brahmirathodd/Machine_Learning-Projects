Python code for conducting Exploratory Data Analysis (EDA) on a bank dataset.  
The dataset contains information about bank customers, such as age, job, marital status, education, balance, etc. 
This code will help you with the dataset, perform basic data exploration, and generate some visualizations.

import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# Load the dataset
data_path = 'data/bank_data.csv'
bank_data = pd.read_csv(data_path)

# Display the first few rows of the dataset
print("First few rows of the dataset:")
print(bank_data.head())

# Summary statistics
print("\nSummary statistics:")
print(bank_data.describe())

# Data types and missing values
print("\nData types and missing values:")
print(bank_data.info())

# Visualizations
# Distribution of Age
plt.figure(figsize=(10, 6))
sns.histplot(bank_data['age'], bins=20, kde=True, color='skyblue')
plt.title('Distribution of Age')
plt.xlabel('Age')
plt.ylabel('Frequency')
plt.show()

# Count of Marital Status
plt.figure(figsize=(8, 5))
sns.countplot(x='marital', data=bank_data, palette='Set2')
plt.title('Count of Marital Status')
plt.xlabel('Marital Status')
plt.ylabel('Count')
plt.show()

# Boxplot of Balance by Education
plt.figure(figsize=(10, 6))
sns.boxplot(x='education', y='balance', data=bank_data, palette='Pastel1')
plt.title('Boxplot of Balance by Education')
plt.xlabel('Education')
plt.ylabel('Balance')
plt.xticks(rotation=45)
plt.show()

# Correlation matrix
plt.figure(figsize=(10, 8))
sns.heatmap(bank_data.corr(), annot=True, cmap='coolwarm', fmt=".2f")
plt.title('Correlation Matrix')
plt.show()

